# Docker Environment Configuration Example
# Copy this file to .env before running docker-compose

# Application
NODE_ENV=production
PORT=3000

# MongoDB - Docker Internal Network
# Note: When using docker-compose, MongoDB URI is automatically set to:
# mongodb://mongodb:27017/ddd-user-api
# This is overridden in docker-compose.yml
MONGODB_URI=mongodb://mongodb:27017/ddd-user-api
EXTRA_API_KEY=your_extra_api_key_here

# Security
BCRYPT_SALT_ROUNDS=10

# Stripe (optional - for future integration)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# IMPORTANT NOTES:
# - For Docker, you don't need to change MONGODB_URI as it's overridden in docker-compose.yml
# - The 'mongodb' hostname refers to the MongoDB container in the Docker network
# - For local development (without Docker), use: mongodb://localhost:27017/ddd-user-api
